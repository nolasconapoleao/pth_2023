#pragma once

#include <string>
#include "curl/include/curl/curl.h"

// Implement a function that makes a GET request to the given URL
std::string get_request(const std::string &request_url, const std::string &userpassword = "") {
}
